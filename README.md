# Ledger

This is the repository to Ledger, an open source Fluxer Leaderboard bot built in Fluxify.

## Setup

Simply fill out your `.env` according to `example.env` and run the bot using `docker compose up --build`. For the debug build run `docker compose up -f compose.dev.yaml` and setup Watch using `w`.